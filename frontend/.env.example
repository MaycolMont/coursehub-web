# URL base de la API del backend. Por defecto se usa `/api` con el proxy
# de Vite hacia http://127.0.0.1:8000. Cambia esto si el backend corre
# en otro host/puerto o si sirves el frontend por separado.
VITE_API_URL=/api
